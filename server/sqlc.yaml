version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "schema.sql"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
        emit_empty_slices: true
        emit_json_tags: true
        emit_db_tags: true
